{
  "name": "awesome-coding-plans-web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "node scripts/split-plans.mjs && node scripts/sync-fonts.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/split-plans.mjs && node scripts/sync-fonts.mjs",
    "build": "astro build && node scripts/build-static.mjs",
    "check": "astro check",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource-variable/space-grotesk": "^5.3.0",
    "@fontsource/dm-mono": "^5.3.0",
    "astro": "^6.4.8",
    "marked": "^15.0.0"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
